<core:FragmentDefinition 
    xmlns="sap.m" 
    xmlns:core="sap.ui.core" 
    xmlns:f="sap.ui.layout.form">
    
    <Dialog id="_IDGenDialog" title="Create New Order" icon="sap-icon://add" contentWidth="400px">
        <content>
            <f:SimpleForm id="_IDGenSimpleForm" layout="ColumnLayout" editable="true">
                <!-- Chọn Sản Phẩm -->
                <Label id="_IDGenLabel2" text="Sản phẩm" required="true" />
                <ComboBox id="_IDGenComboBox" 
                    selectedKey="{newOrder>/product_ID}" 
                    items="{path: '/Products'}"
                    placeholder="Chọn sản phẩm...">
                    <core:Item id="_IDGenItem5" key="{ID}" text="{name}" />
                </ComboBox>

                <!-- Chọn Khách Hàng -->
                <Label id="_IDGenLabel3" text="Customer Name" required="true" />
                <Input id="_IDGenInput" />
            </f:SimpleForm>
        </content>
        <beginButton>
            <Button id="_IDGenButton3" text="Save" type="Emphasized" press=".onSaveNewOrder" />
        </beginButton>
        <endButton>
            <Button id="_IDGenButton4" text="Cancel" press=".onCloseDialog" />
        </endButton>
    </Dialog>
</core:FragmentDefinition>