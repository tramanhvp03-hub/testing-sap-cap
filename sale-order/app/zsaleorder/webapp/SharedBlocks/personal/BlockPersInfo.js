sap.ui.define(['sap/uxap/BlockBase'],
	function (BlockBase) {
		"use strict";

		var BlockPersInfo = BlockBase.extend("zsaleorder.SharedBlocks.personal.BlockPersInfo", {
			metadata: {}
		});


		return BlockPersInfo;

	});
