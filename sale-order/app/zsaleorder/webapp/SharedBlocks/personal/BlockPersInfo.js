sap.ui.define(['sap/uxap/BlockBase'],
	function (BlockBase) {
		"use strict";

		var BlockPhoneNumber = BlockBase.extend("zsaleorder.SharedBlocks.personal.BlockPersInfo", {
			metadata: {}
		});


		return BlockPhoneNumber;

	});
